export interface Employee {
    //the type of data that will be returning because we are going to be making calls to our backend
    id: number;
    name: string;
    email: string;
    jobTitle: string;
    phone: string;
    imageUrl: string;
    employeeCode: string;
}